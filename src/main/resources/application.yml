spring:
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.17.112.179:3306/pawprint_db?serverTimezone=UTC
    username: root
    password: 123456
mybatis-plus:
  configuration:
    # 开启下划线到驼峰命名转换
    map-underscore-to-camel-case: true
    # 调试日志标准输出
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 表前缀
      table-prefix: tbl_
      id-type: auto

